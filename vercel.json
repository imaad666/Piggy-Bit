{
    "buildCommand": "cd piggy-bit && npm install && npm run build",
    "outputDirectory": "piggy-bit/dist",
    "installCommand": "cd piggy-bit && npm install --legacy-peer-deps",
    "framework": "vite"
}